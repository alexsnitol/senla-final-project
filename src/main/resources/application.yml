jwt:
    key: examplesupersecurekeyforencodinganddecodingtheHS256algorithm
spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: postgres
        url: jdbc:postgresql://localhost:5432/real_estate_market
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
    redis:
        host: localhost
        port: 6379
    servlet:
      multipart:
          max-file-size: 10MB
          max-request-size: 10MB
