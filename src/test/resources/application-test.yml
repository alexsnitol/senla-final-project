jwt:
  key: examplesupersecurekeyforencodinganddecodingtheHS256algorithm
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/real_estate_market
    password: postgres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true